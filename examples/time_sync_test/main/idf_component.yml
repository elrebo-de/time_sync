version: 1.0.0
dependencies:
  idf: '>=5.5.0'
  elrebo-de/time_sync:
    version: "1.0.0"
    override_path: '../../../' # three levels up, pointing to the directory with the component itself
  elrebo-de/wifi_manager: ^1.3.0
